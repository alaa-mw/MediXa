export { default } from "./OfferForm";

/*
import Grid from "@mui/material/Grid";
import OfferForm from "../../components/private-offer/OfferForm";

const CreatePrivateOfferPage = () => {
  const handleCreateOffer = (data: OfferFormModel) => {
    console.log(data);
  };

  return (
    <Grid container spacing={3}>
      <Grid item xs={12} lg={6}>
        <OfferForm onSubmit={handleCreateOffer} />
      </Grid>
    </Grid>
  );
};

export default CreatePrivateOfferPage;
*/
